#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift File.join(File.dirname(__FILE__), '..', 'lib')

require 'controler'
require 'null_action'
require 'user_interface'

actions = [NullAction.new]

Controler.new(UserInterface.new($stdin, $stdout), actions).start
